name: example
publish_to: none

environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: ">=3.35.1"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.2.0
  http: ^1.2.0
  csv:
  template_string:
    path: ../../template_string
  localization_builder:
    path: ../